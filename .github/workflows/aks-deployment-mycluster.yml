name: Deploy to mycluster

on:
  push:
    branches: ["master"]
  workflow_dispatch:

jobs:
  deploy_mycluster:
    name: Deploy to mycluster
    permissions:
      actions: read
      contents: read
      id-token: write
    uses: chufe-dremio/dremio-cloud-tools/.github/workflows/aks-deployment.yml@master
    with:
      environment: mycluster

